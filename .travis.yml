notifications:
  email: false
language: node_js
matrix:
  fast_finish: true
node_js:
- '6'
branches:
  except:
  - build
cache:
  directories:
  - node_modules
script:
- npm run lint
- yarn add cozy-app-publish
- yarn cozy-app-publish --travis --build-dir '.' --on-branch master --space mes_infos
