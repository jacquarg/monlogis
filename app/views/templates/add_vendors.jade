mixin vendor(slug)
  li.img-thumbnail.houseitem.objectitem(data-slug=slug)
    img(src="assets/img/icon_konnectors/#{slug}.svg", title=name)

.col-xs-12
  h2 Sélectionnez vos fournisseurs, puis configurez la collect automatique de données (factures, ...)

.col-xs-12.frame
  .row
    .col-xs-12.category.mesinfos
      h3 Fournisseurs&ensp;
        b partenaires MesInfos
      ul
        each v in mesinfos
          +vendor(v.slug)
      .end

    .col-xs-12.category.isp
      h3 Fournisseurs&ensp;
        b Internet
      ul
        each v in isp
          +vendor(v.slug)
      .end

    .col-xs-12.category.telecom
      h3 Fournisseurs&ensp;
        b Télécom
      ul
        each v in telecom
          +vendor(v.slug)
      .end
